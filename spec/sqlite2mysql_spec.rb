require 'spec_helper'

describe Sqlite2mysql do
  it 'has a version number' do
    expect(Sqlite2mysql::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
